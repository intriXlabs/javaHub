A recursion approach based on two execution phases: Outward and Inward. During the outward phase, recursive calls advance the state (such as an index) until the base case is reached. During the inward phase, the recursion unwinds, allowing each call to contribute its result while returning. Problems are viewed as "Current Contribution + Answer of Remaining State," making recursion behave like a state transition system rather than simply a function calling itself.